# 2026-08-13 工作日志

## V6 专家库前端 7 项反馈（Part A + Part B）

### Part A（已 commit 并 push）
8 个分组 commit（区间 6cdc178..83b893a），用户已手动 push：
- 后端 lastLoginAt 字段链路（UserEntity/UserDTO/AuthServiceImpl/init.sql）
- 评分引擎对齐 V5.9.x 五子维度（scoring.ts）
- 评分管理页/文档对照 V5（RatingsTab/ScoringRulesDocModal）
- 前端仪表盘对标 V5（DashboardModal：领域分布去排序、环形图美化、去小字）
- 后台仪表盘（DashboardTab：去描述、标注"不包含观察库专家"、去小字）
- 用户管理精简（UsersTab/types）
- 合作项目工具栏左对齐（ProjectsTab）
- 底部浮动导航压缩（FrontendView）

### Part B（7 项新反馈，本轮全部完成并 commit，区间 83b893a..09826dd）
用户手动 push 一次后提出，本轮清理陈旧任务列表（#254-#264/#223 → completed），新建 #279-#285：
1. #282 底部导航：导航"框"整体收窄为居中胶囊（position fixed 居中 + border-radius 999px），非仅缩小内部页码。
2. #279 前端环形图（DashboardModal）：白色描边 3→2（细间距）、中心数 30/800→24/600（加粗减弱居中）、各段占比文字落扇形环内、去 paint-order 暗描边阴影；分值标签统一为"X.X分"无括号（与后台一致）。
3. #280 极窄屏（isNarrowScreen = innerWidth<=400）专家卡领域标签：displayFieldName 已正确映射 `通用（…）→通用…`、`战略规划/战略解码/战略落地→战略…`；详情页仍显示完整（代码核对无需改动）。
4. #281 合作项目：工具栏左对齐（与"合"对齐）；"新建项目"按钮移到搜索框右侧，导入/导出归入 toolbar-actions 右侧。
5. #284 评分管理：去浅蓝套框（.doc-embed）→ 仅标题+查看完整文档按钮；文档删"综合评分=…×60%+…×40%"公式行；"覆盖…共5个评分项"→"…每项占20%"；表格列"评分项构成"→"每项各占20%"；界面去掉"关键测算结果"测算表（仅留文档）；修复操作列下拉框被 table 滚动容器裁切（改 position:fixed + getBoundingClientRect 动态定位 + scroll 关闭）；预警提示美化（.warn-banner/.warn-icon/.btn-warn）。
6. #283 后台环形图（DashboardTab）：加白色描边 2、段内占比文字、中心数 22/700→24/600（与前台一致）；图例细化两行（区间加粗 + 人数(占比%)），间距 30→34。
7. #285 权限管理：子管理员默认权限调整（expertScore:false=评分仅查看；categoryManage:false；projects/dashboard/observation/expertView/Add/Edit/Import/Export:true）；主管理员去方框→标题行+修改密码按钮（小标题右侧）；子管理员小标题字号与主管理员一致；"数据看板"→"仪表盘"，小字"查看和导出"。

### 验证
vue-tsc --noEmit EXIT:0；vite build EXIT:0（9.85s）。

### 备注
- 后端 Maven 本环境不可用，沿用上轮 lastLoginAt 增量字段（未启动后端）。
- 7 个 commit 已建，待用户 GitHub Desktop push。
